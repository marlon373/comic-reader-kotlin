<resources>
    <!-- Base application theme. -->
    <style name="Theme.ComicReader" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/onyx_heart</item>
        <item name="colorPrimaryVariant">@color/onyx_heart</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!-- Set the default background color -->
        <item name="android:windowBackground">?attr/colorPrimaryVariant</item>

        <item name="preferenceTheme">@style/PreferenceTheme</item>
    </style>
</resources>